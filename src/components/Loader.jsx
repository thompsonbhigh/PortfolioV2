import { Html } from '@react-three/drei';

const Loader = () => {
    return (
        <Html>
            <div class='loader'>
                <div class='loading-anim' />
            </div>
        </Html>
    )
}

export default Loader
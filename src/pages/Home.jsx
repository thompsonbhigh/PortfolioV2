import React from 'react'
import Navbar from '../components/Navbar';

const Home = ({setCurrentStage}) => {
    setCurrentStage(1);
    return (
        <section class='home'>
        </section>
    )
}

export default Home